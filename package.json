{
  "name": "plexbot",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute feedback_aggregator --file=migrations/0001_create_feedback_table.sql",
    "db:studio": "wrangler d1 execute feedback_aggregator --command=\"SELECT name FROM sqlite_master WHERE type='table';\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["slack", "bot", "feedback", "aggregator", "cloudflare", "workers"],
  "author": "",
  "license": "ISC",
  "description": "AI-powered Slack bot for aggregating and analyzing network infrastructure feedback",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260128.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.61.0"
  },
  "dependencies": {
    "hono": "^4.11.7"
  }
}
